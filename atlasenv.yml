channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python >=3.11, < 3.12
  - bbmap >= 39.01, <40
  - snakemake-minimal >= 9.3.3, <10
  - pygments
  - networkx
  - graphviz
  - pandas >=1.2, <1.6
  - pyarrow # for parquet reading
  - click >=7
  - ruamel.yaml >=0.17
  - cookiecutter
  - wget
  - xmltodict
