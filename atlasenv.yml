channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python =3.11
  - mamba
  - bbmap >= 39.01
  - snakemake-minimal >= 8
  #- pygments
  #- networkx
  #- graphviz
  - pandas =2
  - pyarrow # for parquet reading
  - click >=7
  - ruamel.yaml >=0.17
  - cookiecutter
  - wget
