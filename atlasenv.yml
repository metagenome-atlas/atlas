channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python >=3.8, < 3.11
  - mamba
  - bbmap >= 39.01, <40
  - snakemake-minimal >= 7.15.1, <7.16
  - pygments
  - networkx
  - graphviz
  - pandas >=1.2, <1.5
  - pyarrow # for parquet reading
  - click >=7
  - ruamel.yaml >=0.17
  - cookiecutter
  - pathlib
